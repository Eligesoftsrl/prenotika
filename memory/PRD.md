# EligeHub – SaaS gestione appuntamenti per centri studi

## Original problem statement
Convertire un'applicazione legacy Flask + MySQL (Wk) in un SaaS moderno per centri studi / professionisti che gestiscono appuntamenti di clienti/alunni in base alla disponibilità oraria di docenti. Frontend React mobile-friendly, backend Python, DB con chiavi primarie solide. Ogni docente sceglie giorni e fasce di disponibilità per costruire un calendario orario personalizzato.

Codice originale conservato in `/app/uploads/Wk_extracted/` (Flask + MySQL, riferimento - non eseguito).

## Stack
- Backend: FastAPI + Motor (MongoDB) + JWT (PyJWT) + bcrypt
- Frontend: React 19 + Tailwind + lucide-react + react-router 7
- Italiano, mobile-first

## Architettura
- Multi-tenant per `studio_id` con RBAC: `super_admin`, `admin`, `docente`
- UUID `str` come `_id`. Indici unique su: users.email, materie(studio_id,descrizione), docente_clienti(studio_id,docente_id,cliente_id), docente_materie(studio_id,docente_id,materia_id)

## Endpoint principali
- Auth: `POST /api/auth/login`, `GET /api/auth/me`
- Tenant: `GET/POST/DELETE /api/studios` (super_admin)
- Docenti: `GET/POST/PATCH/DELETE /api/docenti` + campo `slot_minuti` (15/30/45/60/90/120)
- Clienti: `GET/POST/PATCH/DELETE /api/clienti` (filtro `?docente_id`, scope automatico per docenti)
- Orari (disponibilità ricorrente): `GET/POST/DELETE /api/orari`
- Appuntamenti: `GET/POST/PATCH/DELETE /api/appuntamenti` (check overlap → 409)
- **Bulk + ricorrenza + nuovo cliente inline**: `POST /api/appuntamenti/bulk` con `slots[]`, `cliente_id` o `nuovo_cliente`. Ritorna `{created, skipped, count_*}`
- Disponibilità slot liberi: `GET /api/disponibilita?docente_id&data&slot_minuti`
- **Materie**: `GET/POST/PATCH/DELETE /api/materie` + N:M `/api/docenti/{id}/materie/{materia_id}` (replica tabella `combomat`)
- **Alunni↔Docenti** N:M: `/api/docenti/{id}/alunni/{cliente_id}` (replica tabella `pubblico`)
- Dashboard: `GET /api/dashboard/stats`

## Pagine frontend
- `/login`, `/dashboard`
- `/appuntamenti` — calendario settimanale, modal potenziato con tab "Studente in archivio / Nuovo studente" + ricorrenza prossime 6 settimane
- `/orari?docente=ID` — editor settimanale
- `/docenti` — CRUD + colonna Durata + pulsanti **Calendario / Alunni / Materie**
- `/docenti/:id/alunni` — gestione alunni associati
- `/docenti/:id/materie` — gestione materie associate
- `/clienti` — CRUD + search
- `/materie` — catalogo materie del centro studi
- `/studios` (super_admin)

## Implementato (12 Giu 2026)
**Iter 1**: backend + frontend MVP completi, seed idempotente. 22/22 backend test.
**Iter 2**: durata appuntamento per docente (`slot_minuti`), relazione N:M alunni↔docenti, pagina dedicata alunni docente, autocalcolo "Al" da slot, filtri. 33/33 test.
**Iter 3**: catalogo Materie + N:M docenti↔materie, modal appuntamento con tab "esistente / nuovo studente" inline, ricorrenza +7..+42 giorni, endpoint `/api/appuntamenti/bulk`, fix centratura modal con scroll. 46/46 backend test + 12/12 frontend dopo fix.
**Iter 4**: 3 tipologie studio (`centro_studi` / `studio_legale` / `studio_medico`), terminologia dinamica via `tipologia.js`, modali con React Portals, PDF report con merging contigui, pagina dedicata `/appuntamenti/nuovo`.
**Iter 5 (Feb 2026)**: Rebrand **Prenotika** + Logo SVG, pagina `/impostazioni` con campo `comunicazioni` (admin) appeso al footer dei PDF generati. Fix nav: link "Impostazioni" aggiunto alla sidebar admin in `Layout.jsx`. Nuova pagina **/report** (admin) per generare PDF planning aggregato giorno/settimana/mese di tutti i professionisti o singolo, con date picker e selettore docente. Upload **logo studio** in Impostazioni (PNG/JPG/WEBP/SVG max 600KB) salvato come base64 e stampato in cima a tutti i PDF (validazione PIL, fallback silente se corrotto).
**Iter 6 (Feb 2026)**: Bug fix P.IVA in Impostazioni (RCA: `EmailStr` rifiutava `''` con 422 → fix: frontend normalizza `''→null` per campi opzionali, mantiene `''` per `logo_base64`/`comunicazioni` come cancella esplicito). Carta intestata PDF: nuovo header con logo (sx) + nome studio, sede, telefono • email, P.IVA (dx) + linea separatrice verde brand. Test iter11 100% (6/6 backend + 3/3 frontend).
**Iter 7 (Feb 2026)**: Rebrand completo seguendo Prenotika Brand Guidelines v1.0. Palette: Electric Indigo `#7C3AED`, Neo Cyan `#2DD4BF`, Soft Sky `#60A5FA`, Midnight `#0F172A`, Light Neutral `#F8FAFC`. Font: **Sora** (display) + **Inter** (body) + **Manrope** (fallback). Logo PNG ufficiale (`/prenotika-icon.png`). Login dark hero con 3 blob animati + headline brand "La gestione **intelligente** degli appuntamenti". Sidebar/Layout/PDF aggiornati con la nuova palette. Test iter12 100% (6/6 backend + 3/3 frontend, nessuna regressione). Rimossi i claim "SaaS" dal copy pubblico.
**Iter 8 (Feb 2026)**: Integrazione **Brevo Transactional Email + ICS**. Nuovo modulo `email_service.py` con: build_ics() conforme RFC 5545 (METHOD:REQUEST, UID, DTSTAMP, DTSTART/DTEND in UTC, ATTENDEE/ORGANIZER), html_body brandizzato Prenotika (gradient header viola/cyan/teal), invio async via httpx a Brevo `/v3/smtp/email`. Hook in `POST /api/appuntamenti` (singolo) e `POST /api/appuntamenti/bulk` (multi-evento in un unico VCALENDAR). Fail-safe: errori loggati ma appuntamento creato comunque. Env: `BREVO_API_KEY`, `BREVO_SENDER_EMAIL=team@zioners.com`, `BREVO_SENDER_NAME=Prenotika`. Test: HTTP 201 da Brevo confermato sia per singolo che bulk.
**Iter 9 (Feb 2026)**: Quattro feature insieme. (a) **Landing page pubblica** `/` con hero brand (3 blob animati, gradient text), sezione Funzioni (6 card), "Per chi" (centri studi/legali/medici), Contatti con form lead POST `/api/leads` (non-auth) + notifica Brevo a team@zioners.com. (b) **Email disdetta**: hook DELETE `/api/appuntamenti/:id` invia ICS METHOD:CANCEL + HTML stile dark. (c) **Scheduler reminder 24h**: nuovo `reminder_scheduler.py` con APScheduler AsyncIO, cron(minute=5) cerca appuntamenti tra +23h/+25h non ancora avvisati, invia email con `reminded_at` marker (TZ Europe/Rome). (d) **Ferie/Eccezioni**: nuova entità `eccezioni` con CRUD `/api/eccezioni`, tipo chiuso/personalizzato, validate in POST appuntamenti (singolo + bulk → skip o 409). Frontend: pagina `/eccezioni` (admin+docente) con form, lista, link sidebar "Ferie". Test iter13 100% (11/11 backend + UI flows + regressioni).
**Iter 10 (Feb 2026)**: Permission tightening + Pricing vetrina. (a) **DELETE /api/appuntamenti/:id** ristretto a `admin` only (era admin+docente). Frontend Appuntamenti: pulsante Trash nascosto a docente sia in vista settimana che giorno. (b) **Sezione Prezzi** nella landing tra Per chi e Contatti: 3 piani (Free €0, Pro €29 con badge "Più scelto" e card dark gradient, Business €79), CTA "Inizia gratis 14 giorni" su Pro che scrolla al form e pre-compila `piano_interesse` + messaggio. Form lead esteso con `piano_interesse` + badge visivo, propagato in email notifica Brevo. Test self via curl: DELETE doc=403, admin=204, POST lead+piano=201.
**Iter 11 (Feb 2026)**: **Landing "wow 2026"**. Riscritta usando `framer-motion`. Effetti: (a) hero dark con **aurora gradient mesh** animato + dot grid + **spotlight cursor-tracking** (radial gradient che segue il mouse) + **3 floating cards** mock (visita, promemoria, settimana) con animazioni y up/down + **live ticker** prenotazioni recenti con AnimatePresence + scroll indicator + parallax scroll-Y. Headline con **stagger reveal** word-by-word. (b) **Stats counters** animati on viewport (12k appuntamenti, 320 studi, 24/7, 99.9% uptime). (c) **Marquee** scroll continuo con integrations. (d) **Bento grid** features con card grande "Calendario per professionista" + 6 card secondarie con glow hover. (e) **Pricing 3D tilt** mouse-tracking + magnetic buttons. (f) **Testimonial** card stelle. (g) **CTA finale** dark gradient con magnetic CTA. Aggiunti in CSS: `.aurora-mesh`, `.marquee-track`, animazioni keyframes.
**Iter 12 (Feb 2026)**: Pricing onesto + testimonial reale. RCA: l'utente ha chiesto se le limitazioni piano Free fossero applicate — risposta: **NO** (nessun enforcement tecnico). Quindi rimosse le esclusioni sbarrate (Email auto, PDF brandizzati, ferie) dal piano Free; ridifferenziati i piani solo per dimensione/supporto (Free: 1 prof, Pro: 5 prof + reminder, Business: illimitato + branding + SLA + API). Aggiunta nota trasparenza "Tutte le funzioni sono incluse in ogni piano". Testimonial cambiato a **Francesca · Centro Studi Arco Iris** (utente reale della vecchia versione). Ticker hero aggiornato con riferimento ad Arco Iris.
**Iter 13 (Feb 2026)**: **Piani con enforcement reale**. Prezzi aggiornati: Pro **€14**, Business **€24** (era €29/€79). Testimonial Francesca ampliata (sostituzione planning cartacei + memoria umana). Backend: aggiunto campo `plan` (Free/Pro/Business) a `StudioBase`. `PLAN_LIMITS_PROFESSIONISTI = {free:1, pro:5, business:None}`. Hook in `POST /api/docenti`: HTTP 403 se count >= limit. Nuovo endpoint `GET /api/studio/quota`. Nuovo endpoint `PATCH /api/studios/:id` (super_admin) per cambiare il piano. **Security**: in `PATCH /api/studio` (admin) il campo `plan` viene filtrato server-side → impossibile self-upgrade. Frontend: pagina Studios con colonna Piano + dropdown inline + selettore nel form di creazione. Pagina Docenti con `quota-indicator` + banner `quota-warning` + bottone "Nuovo" disabled se al limite. Studio demo seed → plan=business per test smooth. Test iter14 100% (12/12 backend + UI + 17/17 regressioni).
**Iter 14 (Feb 2026)**: **Area Leads + Upgrade modal + Badge sidebar**. (a) Backend nuovi endpoint super_admin: `GET /api/leads`, `GET /api/leads/count?status=new`, `PATCH /api/leads/:id` (status new/contacted/converted/closed + notes), `DELETE /api/leads/:id`. (b) Nuova pagina frontend `/leads` (super_admin) con lista + filtri per stato + search + pannello dettaglio con quick actions (cambia stato, mailto rispondi, elimina). Link "Richieste" (icon Inbox) in sidebar super_admin **con badge contatore** (gradient rosso→arancio) che polling ogni 45s il numero di lead con status="new" e ad ogni cambio route. (c) **Upgrade modal elegante** in `/docenti`: quando l'admin è già al limite quota, cliccare "Nuovo docente" apre direttamente un Modal brandizzato invece del form → card dark gradient con piano suggerito (Pro €14 o Business €24), CTA mailto team@zioners.com "Contatta il team di Prenotika". Anche l'intercept 403 (path fallback) apre lo stesso modal. Bottone create disabled durante il caricamento della quota (fix race condition). Test iter15 100% (6/6 backend + 29/29 regressioni + UI happy path).

**Iter 15 (Feb 2026 – Deploy cleanup)**: **Produzione pulita per Railway**. (a) Rimosso l'intero seed demo (`Centro Studi Demo`, `Studio Legale Demo`, `Studio Medico Demo` + admin/docenti/orari/clienti/materie). Ora `on_startup` chiama solo `ensure_indexes()` + `seed_super_admin()` (idempotente). (b) Rimosse le variabili `DEMO_STUDIO_NAME`, `DEMO_ADMIN_EMAIL/PASSWORD`, `DEMO_DOCENTE_EMAIL/PASSWORD`, `DEMO_STUDIO_ID` da `.env`. (c) Aggiunti indici `eccezioni` e `leads` mancanti. (d) Rimossi i pulsanti "Admin Demo / Docente Demo / Super Admin" dalla `Login.jsx`. (e) Nuovo `backend/.env.example` con tutte le variabili documentate per Railway. (f) Nuovo script `backend/scripts/init_railway_db.py` che pinga Mongo, crea gli indici e fa seed del super_admin senza avviare il server.

**Iter 16 (Feb 2026 – Legal + UX)**: **GDPR + Cookie banner + Rimozione Emergent**. (a) Rimossi tutti i riferimenti "Made with Emergent" da `public/index.html` (badge, script `emergent-main.js`, tracking PostHog). (b) Nuovo componente globale `CookieBanner.jsx` con "Solo essenziali / Accetta tutto", persistenza `localStorage`, animato Framer Motion. (c) Nuova pagina `/privacy` (informativa GDPR completa: titolare Eligesoft Srl P.IVA 04532690650, dati raccolti, base giuridica consenso, diritti art.15-22 GDPR, contatti privacy@eligesoft.com). (d) Checkbox consenso GDPR obbligatorio nel form lead landing, con link `/privacy`. (e) Nuovo footer landing con "Prenotika è un prodotto sviluppato da Eligesoft Srl · P.IVA 04532690650". (f) `api.js`: warning console chiaro se `REACT_APP_BACKEND_URL` non è settato. (g) Fix build Vercel con `CI=true`: rimessi 7 `eslint-disable-line react-hooks/exhaustive-deps` legittimi + 1 refactor `setDocenteId((prev) => prev || id)` per rimuovere dep implicita.

**Iter 17 (Feb 2026 – Account & Rename SuperAdmin UI)**: (a) Nuovo endpoint `POST /api/auth/change-password` (verify current password + validate min 8 chars + update hash). (b) Nuovo `ChangePasswordRequest` Pydantic model. (c) Frontend: toggle 👁️ / 👁️‍🗨️ mostra/nascondi password in `/login` con icona Eye/EyeOff lucide-react. (d) Nuova pagina `/account` (accessibile a tutti i ruoli inclusi super_admin) con card "Dati profilo" (Nome, Email, Ruolo) + card "Cambia password" (3 campi con toggle occhio per ciascuno, validazione lato client su match + lunghezza). (e) Link "Account" (icona UserCog) aggiunto in sidebar per tutti i ruoli. (f) Rinominato tutto ciò che era "Centri Studi / Centro Studi / Nuovo centro" in "Aziende / Azienda / Nuova azienda" nella super_admin UI (Layout sidebar + pagina Studios: titolo, sottotitolo, CTA, empty state, modal title, label form). (g) Rimosso bug script legacy che causava crash backend (`}, {"$set": {"plan": "free"}})` rimasto orfano dopo la pulizia demo). Tutti gli endpoint testati via curl → 200 su change + re-login + revert.

**Iter 18 (Feb 2026 – Password recovery via email)**: (a) Cambiato sender Brevo da `team@zioners.com` a `booking@prenotika.com` (default + notify lead + `.env`/.env.example). (b) Nuovi endpoint pubblici `POST /api/auth/forgot-password` (genera token URL-safe 32 byte, salva in `password_resets` collection con expiry 60 min, invia email Brevo con magic link; sempre risponde 200 per privacy) e `POST /api/auth/reset-password` (verifica token valido/non usato/non scaduto → aggiorna password + marca token come used). (c) Nuova collection `password_resets` con indice unique su `token` + indice su `expires_at`. (d) Nuova funzione `send_password_reset_email()` in `email_service.py` con template HTML brandizzato (gradient Prenotika, CTA + fallback URL testo). (e) Nuove pagine frontend `/forgot-password` (email + stato "Controlla la tua email") e `/reset-password?token=XXX` (nuova password + conferma con toggle occhio, redirect automatico al login dopo successo). (f) Link "Password dimenticata?" aggiunto sotto il bottone Accedi nel `/login`. (g) Nuova env `FRONTEND_URL` (con default `https://www.prenotika.com`) usata per costruire il magic link. Testato flusso end-to-end via curl: forgot → 200 + Brevo 201 Created (email consegnata), reset con token valido → 200, reset con token già usato → 400 "Link già utilizzato", login con nuova pwd → 200, forgot con email inesistente → 200 (privacy-safe). Testato UI: entrambe le pagine renderizzano correttamente con brand Prenotika.

**Iter 19 (Feb 2026 – Welcome email on Studio creation)**: (a) Nuova funzione `send_welcome_admin_email()` in `email_service.py` con template HTML brandizzato. (b) `POST /api/studios` ora, dopo aver creato l'admin dello studio, genera un `password_resets` token 7-giorni e invia email di benvenuto tramite Brevo. (c) **Best-effort**: try/except — se l'email fallisce la creazione studio va comunque a buon fine.

**Iter 20 (Feb 2026 – Edit Studio + Nuovo Appuntamento UX/UI overhaul)**: (a) Super_admin: pulsante **Modifica** su `/studios`. (b) `/api/disponibilita` ora considera anche eccezioni/ferie. (c) `/appuntamenti/nuovo` completamente ridisegnato: hero gradient, step numerati, slot picker visuale (solo orari liberi), card riepilogo live, grid ricorrenza 6 settimane con verifica disponibilità.

**Iter 21 (Feb 2026 – Report per studente + Week preview)**: (a) **`GET /api/reports/appuntamenti.pdf`** ora accetta `cliente_id` opzionale: se settato genera un PDF specifico per quel cliente/studente (tabella cronologica con Data / Giorno / Dalle / Alle / Docente / Corso / Stato / Note + intestazione con nome + contatti + totale). I docenti possono generare solo report dei clienti a loro associati (403 altrimenti). (b) Pagina `/report` estesa: nuovo **toggle "Per professionista / Per studente"** che cambia il selector di destra (dropdown docente OR dropdown studente); il download URL usa `cliente_id` o `docente_id` a seconda della modalità. Il super_admin non ha `/report` (già così), l'admin/docente sì. (c) Pagina `/clienti`: nuovo pulsante **FileText "Report"** per ogni riga (visibile a tutti i ruoli, non solo admin) che apre un mini-modal con selettore periodo (Giorno/Settimana/Mese) + data + download PDF. Anche i docenti possono generare report dei propri studenti direttamente da qui. (d) `/appuntamenti/nuovo`: nuova **anteprima settimana** sotto il campo Data → griglia 7 giorni della settimana corrente con conteggio slot liberi per giorno (dot verde con "N" se disponibili, "0" grigio se pieno). Click sul giorno → seleziona quella data e ricarica slot. Fetch parallelo di `/api/disponibilita` per ogni giorno della settimana. Testato: PDF cliente 2418 bytes (`%PDF-1.4` valid), PDF docente 2445 bytes valid.

**Iter 22 (Feb 2026 – Onboarding automatico + OTP passwordless + Stripe checkout)**: nuovi endpoint `POST /api/onboarding/start`, `GET /api/onboarding/verify-token`, `POST /api/onboarding/complete`, `POST /api/auth/otp/request`, `POST /api/auth/otp/verify`; email `send_otp_email()` e `send_onboarding_start_email()`; Stripe checkout via emergentintegrations (POST /payments/checkout/session, GET /status/{id}, POST /webhook/stripe, GET /payments/plans); nuove pagine `/onboarding/setup` (wizard 5 step) e `/login/otp`; landing ora crea automaticamente tenant al submit; sezione Piano & abbonamento in `/account` per admin.

**Iter 23 (Feb 2026 – Terminologia dinamica + Etichette custom + Trial 30gg + Cleanup)**: (a) Fix massiccio della terminologia dinamica basata su `studio.tipologia`: rimosse ~15 stringhe hardcoded ("Docente/Alunni/Materie") in Layout, Dashboard, Docenti, Materie, Orari, Appuntamenti, Report, Clienti, DocenteAlunni, DocenteMaterie, NuovoAppuntamento — ora tutti usano `tipologiaLabels(studio?.tipologia, studio?.custom_labels)`. Uno studio_medico vede "Medici/Pazienti/Specializzazioni" nel menu, dashboard, empty state, modal, ecc. (b) **Etichette personalizzate**: nuovo campo `custom_labels` sullo Studio (dict opzionale con chiavi: cliente, clienti, docente, docenti, materia, materie, materie_label, associa_alunno). L'admin dalle Impostazioni può sovrascrivere le label di default della sua tipologia — es. studio di psicoterapia → "Terapeuta/Assistito", centro fitness → "Coach/Atleta". (c) `tipologiaLabels()` ora accetta un secondo parametro `custom` che fonde gli override con i default (solo valori non-vuoti vengono usati). (d) Nuova sezione UI in `/impostazioni` "Etichette personalizzate" con 8 input + pulsante "Ripristina default della tipologia". (e) Landing form ulteriormente semplificato: solo Email + Tipo studio + Piano (card Free/Pro/Business cliccabili) + privacy. Rimossi Nome, Telefono, Azienda, Messaggio (opzionali nel backend, nome fallback dalla parte locale email). Titolo "Inizia in 30 secondi", CTA "Attiva prova gratuita". (f) **Trial gratuito 30 giorni** su ogni piano a pagamento: nuovo tenant Pro/Business parte con `plan=piano_scelto` + `trial_active=true` + `trial_ends_at=+30d`. Al login viene chiamato `_enforce_trial_expiry()`: scaduto senza pagamento → downgrade a Free; se ha pagato tramite Stripe → `trial_active=false` e piano permanente. Banner "Prova gratuita PRO attiva · X giorni rimanenti" in `/account`. (g) Fix prezzi Stripe: Pro **14€**/mese, Business **24€**/mese (allineati alla landing). Features `/api/payments/plans` allineate 1:1 allo screenshot. CTA Business rinominata da "Parla col team" → "Inizia gratis 30 giorni". (h) DB pulito: tutti i tenant di test rimossi, mantenuto solo super_admin `info@eligesoft.com`. Testato via curl: onboarding_start (payload minimo) → verify-token → PATCH /studio custom_labels → studio con `{docente:"Terapeuta",cliente:"Assistito",…}` correttamente salvato.: (a) Nuovi endpoint pubblici `POST /api/onboarding/start` (crea Studio + admin user + onboarding_token 7g + OTP fallback; se email esiste → invia solo OTP, nessun duplicato tenant, rate-limit 60s), `GET /api/onboarding/verify-token?token=` (ritorna JWT temporaneo + user + studio per accesso wizard senza password), `POST /api/onboarding/complete` (salva studio config + opzionale password, marca token come used, blocca reuse). (b) Nuovi endpoint OTP `POST /api/auth/otp/request` (privacy-safe, sempre 200; genera codice 6 cifre bcrypt-hashed, expire 10min, rate-limit 60s per email) e `POST /api/auth/otp/verify` (max 5 attempts, ritorna JWT). (c) Nuove funzioni email `send_otp_email()` e `send_onboarding_start_email()` in `email_service.py` con template HTML brandizzato (celle codice OTP, gradient Prenotika). (d) Stripe integration via `emergentintegrations.payments.stripe.checkout.StripeCheckout` (test mode): endpoint `POST /api/payments/checkout/session` (admin only; piani Pro 29€/mese, Business 89€/mese hardcoded server-side), `GET /api/payments/checkout/status/{session_id}` (poll con auto-upgrade studio.plan al primo `paid`), `POST /api/webhook/stripe`, `GET /api/payments/plans` (listing pubblico). Chiavi Stripe test in `.env`. (e) Nuove pagine frontend: `/onboarding/setup?token=` wizard 5-step (welcome → studio+tipologia → logo+comunicazioni → password opzionale → done) con skip in ogni step, progress bar animato Framer Motion, redirect a /dashboard al termine. `/login/otp` con phase email → phase code (6 input singole con auto-focus, paste-fill 6 cifre, resend cooldown 45s). (f) Landing `/` `submit()` ora POSTa `/api/onboarding/start` invece di `/leads` → redirect automatico a `/onboarding/setup?token=` per nuovi tenant, a `/login/otp?email=` per email già registrata. (g) `/account` per role=admin: nuova sezione "Piano & abbonamento" con card Pro/Business e CTA Stripe checkout; polling status al ritorno da Stripe con `stripe_session_id` in query. (h) Link "Accedi con codice OTP" aggiunto sotto il login form. (i) Nuove collections MongoDB: `otp_codes`, `onboarding_tokens`, `payment_transactions`. Testing agent 18/18 backend + 10/10 frontend flows passati.

## Backlog (priorità)
- **P1**: Refactor `server.py` (~2500 righe) in router modulari `/app/backend/routes/*` (auth, onboarding, payments, appuntamenti, orari, studios)
- **P1**: Stripe subscriptions full (recurring, webhook renewal, cancellation, portal)
- **P1**: Rimuovere handler duplicato `@app.on_event('startup')` e `@app.on_event('shutdown')` in server.py
- **P1**: pagina pubblica self-booking clienti finali (link condivisibile `/book/[studio]/[docente]`)
- **P2**: vista mensile + drag&drop, date picker localizzato dd/mm/yyyy
- **P2**: cross-modulo: assegnazione materia + alunno in fase di prenotazione
- **P3**: branding personalizzabile per tenant
- **P3**: dashboard admin uso quota vs piano attivo (upgrade prompt inline)

## Credenziali
Vedi `/app/memory/test_credentials.md`
